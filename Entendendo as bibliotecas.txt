BeautifulSoup:

1 - pip install beautifulsoup4 requests ( instalacao do bs e do requests )

2 - import requests ( esse codigo ira obter o conteudo da pagina)
    url = url do wikipedia
    response = requests.get(url)
    html_content = response.content

3 - from bs4 import BeautifulSoup ( esse codigo ira gerar um objeto )
    soup = BeautifulSoup4(html_content, 'html.parser')

4 - table = soup.find('table', {'class': 'wikitable'}) (navegar e extrair dados)

Pandas:

1 - pip install pandas ( instalacao do pandas )

2 - import pandas as pd ( criar dataframe do pandas )
    data = {}
    df = pd.DataFrame(data)

3 - 